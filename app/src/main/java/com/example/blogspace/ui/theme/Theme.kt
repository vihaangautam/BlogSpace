package com.example.blogspace.ui.theme

import android.app.Activity
import com.example.blogspace.R

object Theme {

    fun applyTheme(activity: Activity) {
        activity.setTheme(R.style.Theme_BlogSpace)
    }
}